---
id: DEC.2026-09-21.PULL-LAYS-THE-DUMP-OVER-THE-DIRECTORY
status: planned
governs: product
realized: null
supersedes: []
superseded-by: null
establishes: []
changes: [CTR.WIRE.DUMP-DATA, INV.USE-CASES.REPLACING-A-USER-DIRECTORY-ASKS-FIRST]
---

# `pull` кладёт выгрузку поверх каталога

**Решение.** `pull` кладёт выгрузку поверх каталога пообъектно, как делает платформа по
изменившемуся; чего в базе нет, остаётся на месте. Слияния версий нет: историю,
трёхстороннее слияние и разметку конфликтов держит система контроля версий, раннер
делает расхождение видимым и безопасным. `pull --force` приводит каталог ровно к базе,
лишнее исчезает. Для формата EDT слияния нет ни в одном режиме: выгрузка либо заменяет
проект по тем же правилам подтверждения, либо отказывает. Ответ называет запрошенный
режим и случившийся. Сторож безвозвратного срабатывает и на пообъектную перезапись, а не
только на замену каталога.

**Почему.** Сегодня «слить» и «заменить» — не выбор, а следствие `--mode`: полная
выгрузка заменяет каталог, инкрементальная дописывает, и человек, выбирая скорость,
выбирает судьбу своих правок.

**Не затрагивает.** Сам вопрос к системе контроля версий и его ответы —
`DEC.2026-09-21.UNRECOVERABLE-WORK-IS-ASKED-OF-VERSION-CONTROL` и
`DEC.2026-09-21.AN-UNTRACKED-DIRECTORY-IS-REFUSED-NOT-REPLACED`; `pull --all` и
расширения без проекта — предмет расширений.

Источник: [`cli.html#map`](../../../docs/site/cli.html#map),
[`sources.html#runner`](../../../docs/site/sources.html#runner),
[`problems.html#not`](../../../docs/site/problems.html#not).
