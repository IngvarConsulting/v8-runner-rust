---
id: INV.USE-CASES.AN-EXECUTOR-GAP-IS-NAMED-IN-THE-DOCS
check: []
gap: https://github.com/IngvarConsulting/v8-runner-rust/issues/298
---

# Пробел в матрице исполнителей назван в документах

Операция, у которой на файловой базе и кластере есть исполнитель `designer`, но нет `ibcmd` —
ни готового, ни экспериментального, — или наоборот, названа в разделе «Пока не
поддерживается» `docs/CAPABILITIES.md` строкой «`<команда>` через `<исполнитель>`».
