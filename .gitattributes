README.md text eol=lf
LICENSE text eol=lf
FORK_NOTICE.md text eol=lf

# Перевод строки в записях правил не проверяет ни один гейт: `arch_rules.rs` ищет
# `"---\n"` в байтах и на CRLF блока полей уже не видит. Строку держит поэтому git.
spec/arch/rules/**/*.md text eol=lf
