# IBConnectionString

## Назначение

Передает строку соединения с информационной базой целиком.

## Синтаксис

```text
/IBConnectionString <строка соединения>
```

## Связи

- `/IBConnectionString` должен стоять в командной строке раньше `/S` и `/F`, если они используются для переопределения частей строки соединения.
- Части строки соединения могут быть переопределены параметрами `/S` и `/F`.
- Саму строку соединения нужно брать в кавычки, а внутренние кавычки удваивать.
- В тонком клиенте строка соединения может включать параметры `wsn`, `wsp`, `wspauto`, `wspport`, `wspuser`, `wsppwd`, `wspsrv` для аутентификации и прокси на веб-сервере.

## Примечания

- Значение параметра соответствует формату, который возвращает функция `СтрокаСоединенияИнформационнойБазы()`.

## Расхождения Docs/Runtime

| Спорный пункт | Docs | Observed runtime | Verdict |
| --- | --- | --- | --- |
| Реквизиты `/N`/`/P` рядом с `/IBConnectionString` серверной базы | Справка говорит только о переопределении частей строки ключами `/S` и `/F`; про `/N`/`/P` молчит | Windows, 8.3.27.1936 (отчёт #55, 2026-08-17): `DESIGNER /IBConnectionString 'Srvr="…";Ref="…";' /N <user> /P <pwd>` — «Пользователь ИБ не идентифицирован»; `DESIGNER /S "<server>\<ref>" /N <user> /P <pwd>` — подключается, `/CheckConfig -ConfigLogIntegrity` завершается кодом 0 | runtime wins: объявленный серверный адрес раннер отдаёт как `/S host[:port]\ref`, реквизиты — отдельными `/N`/`/P` |
| То же для файловой базы: `/IBConnectionString File=… /N … /P …` | — | Не замерено | unconfirmed — запрошено в #55 |
| `/S host:port\ref` с портом в хосте | `S.md` описывает адрес как `<компьютер>\<имя базы>`, про порт молчит | Не замерено; в конфигурациях и на сайте `Srvr=srv:1541` — норма | unconfirmed — запрошено в #55 |
| Список резервных серверов в адресе `/S` (`Srvr='srv1,srv2:1641'`) | `S.md` знает у `/S` одну машину; про список молчит | Не замерено | unconfirmed — запрошено в #55; до замера такая строка отдаётся целиком через `/IBConnectionString`, как отдавалась раньше |
